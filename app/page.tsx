import ServicesScrollClient from "@/components/ServicesScrollClient";

export default function Home() {
  return (
    <main style={{ background: "var(--dark)", minHeight: "100vh" }}>
      <ServicesScrollClient />
    </main>
  );
}
